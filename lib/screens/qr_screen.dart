import 'package:flutter/material.dart';

class QrScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Scanner QR Code')),
      body: Center(
        child: Text('Fonctionnalité de scan QR à implémenter'),
      ),
    );
  }
}
